# 2026-07-02 工作日志

## 架构重构：Agentic RAG 三层架构（工具-编排-记忆）

### 今日完成

1. **调研**：研究 Agentic RAG 行业最佳架构实践，确定"编排层-工具层-记忆层"三层架构
2. **创建 `src/retrieval/`**：检索独立切片（含 hybrid/vector/bm25/boosting/base + reranker）
3. **创建 `src/generation/`**：生成独立切片（含 generator/prompt/prompt_builder/warnings + citation）
4. **创建 `src/agent/`**：编排层切片（RAGEngine → RAGAgent、confidence、api）
5. **删除 `src/query/`**：旧模块已不存在，功能分散到三个新切片
6. **创建 ORM 模型**：`src/documents/models.py`（documents schema）和 `src/conversation/models.py`（conversation schema）
7. **数据库 Schema 化**：`init_db.py` 支持 CREATE SCHEMA + 按 schema 建表
8. **修复循环导入**：消除 agent ↔ documents ↔ conversation 循环依赖
9. **修复测试**：更新 10+ 测试文件路径，374/378 通过（4 GPU 跳过）
10. **数据库迁移**：修复 `init_db.py` 未导入 slice models 导致 `create_all` 不知晓新表的问题；5 张表成功创建在 documents/conversation schemas 下
